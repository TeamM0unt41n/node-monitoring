---
apiVersion: 1

datasources:
  - name: prometheus
    type: prometheus
    url: http://prometheus:9090/prometheus/
    access: proxy
    isDefault: true # Set Prometheus as the default data source

#   - name: Loki
#     type: loki
#     url: http://loki:3100
#     access: proxy # Use proxy access for Loki
